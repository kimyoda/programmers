// 1. 공백 한개로 구분되어있는 문자열 my_string
// 2. 앞에서부터 순서대로 담은 문자열 배열 return
function solution(my_string) {
    // 1. 단어의 공백을 분리하고 단어를 순서대로 담는다.
    const words = my_string.split(" ");
    
    return words;
}